
import styled from 'styled-components';

const GrayStripe = styled.div`
    background: linear-gradient(-135deg, lightgray, #aaa);
    box-sizing: border-box;
    padding: 15px calc(2% + 7px);
`

export default GrayStripe;